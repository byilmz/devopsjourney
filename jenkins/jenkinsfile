// template pipeline

pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                git (url: 'https://github.com/byilmz/devops' branch: 'dev')
            }
        }
        stage('Test') {
            steps {
                git (url: 'https://github.com/byilmz/devops' branch: 'dev')
            }
        }
        stage('Deploy') {
            steps {
                git (url: 'https://github.com/byilmz/devops' branch: 'dev')
            }
        }
    }
}